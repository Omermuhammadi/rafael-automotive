"""Multi-Vehicle Diagnostic & Calibration Tool (MVP).

Two pillars in scope:
  * Binary Studio  (Pillar A) — ECU binary read / hex view / patch / checksum / UDS bridge.
  * OCPP Sniffer   (Pillars C+D) — mock CSMS, simulated charge point, triage, remote reset.

Launched from the project root with ``python -m src.main``.
"""

__version__ = "0.1.0"
