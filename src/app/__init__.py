"""UI layer: the CustomTkinter shell, theme, and (from later phases) the panels.

Only modules under ``app/`` and the ``*/panel.py`` files import a UI toolkit. Everything
else stays headless-testable (CLAUDE.md Section 7).
"""
